{
  "name": "ranta",
  "displayName": "Ranta",
  "description": "Tee / Ranta：extension.json（data / process / lambda / event / widget / component）与 Vue 模板、data 访问之间的跳转。",
  "version": "1.0.0",
  "icon": "images/icon.png",
  "publisher": "mo9on",
  "author": {
    "name": "九月",
    "url": "https://github.com/zxccvvb"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/zxccvvb/Ranta.git"
  },
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onLanguage:vue",
    "onLanguage:json"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "ranta.goToWidget",
        "title": "Ranta: Go to Definition"
      }
    ],
    "keybindings": [
      {
        "command": "ranta.goToWidget",
        "key": "alt+f12",
        "when": "editorTextFocus && (editorLangId == vue || editorLangId == json || editorLangId == jsonc)"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "vsix": "npx @vscode/vsce package",
    "version:patch": "npm version patch --no-git-tag-version --no-git-checks",
    "version:minor": "npm version minor --no-git-tag-version --no-git-checks",
    "version:major": "npm version major --no-git-tag-version --no-git-checks",
    "release:patch": "npm version patch",
    "release:minor": "npm version minor",
    "release:major": "npm version major"
  },
  "devDependencies": {
    "@types/node": "^16.18.0",
    "@types/vscode": "^1.74.0",
    "typescript": "^4.9.0"
  }
}
